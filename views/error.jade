extends layout

block content

    header.portrait

        .bg-line
        .body-portrait
            .box-note.error
                #slider-note
                    .carousel-inner
                        .item.active!= '<strong>Erreur ' + statusCode + '</strong> - ' + message

            .text-center
                button(type='button' class='btn btn-flat style2' onclick='window.location.href="/"') Retour à l'accueil

            if error.stack
                .deviter
                pre= '[ STACK TRACE ] ' + error.stack
